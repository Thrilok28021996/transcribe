# Internal APIs

SpeechRecognizer

transcribe(audio)

AlignmentEngine

align(transcript)

DiarizationEngine

diarize(audio)

SpeakerIdentifier

identify(segment)

KnowledgeExtractor

extract(transcript)

MarkdownExporter

export(meeting)

EmbeddingProvider

embed(document)

LLMProvider

generate(prompt)
